import SpaceScreen from '../../components/SpaceScreen';

/** "Notre Espace" : toutes les tâches communes du couple. */
export default function NotreEspace() {
  return <SpaceScreen space="ours" emptyLabel="Aucune tâche commune pour le moment." />;
}
