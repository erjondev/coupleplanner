import ProposalsScreen from '../../components/ProposalsScreen';

/** "Propositions" : activités proposées au sein du couple (reçues / envoyées). */
export default function Propositions() {
  return <ProposalsScreen />;
}
